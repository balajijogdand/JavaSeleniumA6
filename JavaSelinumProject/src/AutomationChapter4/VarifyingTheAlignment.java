package AutomationChapter4;

import org.openqa.selenium.By;
import org.openqa.selenium.Dimension;
import org.openqa.selenium.Point;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class VarifyingTheAlignment {
	public static void main(String[] args) {
		/*WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
		driver.get("https://www.facebook.com/");
		WebElement emailtextbox = driver.findElement(By.id("email"));
		WebElement passwordtextbox = driver.findElement(By.id("PassContainer"));
		
		//feacting the 
	   Dimension emailtextboxsize = emailtextbox.getSize();
	   Dimension passtextboxsize = passwordtextbox.getSize();
	   
	   Point emailtextboxloc = emailtextbox.getLocation();
	   Point passtextboxloc = passwordtextbox.getLocation();
	// varifying the left aligment
		if(emailtextbox.getx() == passwordtextbox.getX())
			System.out.println("Left alignment is proper");
		else
			System.out.println("Left alignment is improper");
		
		// varifying right allinment
		if(emailtextbox.getx()+emailtextboxsize.getWidth() == passwordtextbox.getx()+ passwordtextboxsize.getWidth());
		   System.out.println("Right alingment is proper");
		else
			System.out.println("Right alignment is improper");
		
		// varifing overlappint essue
		
		if()
		*/
	}

}
